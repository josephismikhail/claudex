---
title: One Claude Code session, multiple providers
description: Connect providers locally and switch models without replacing Claude Code
template: splash
hero:
  title: Claudex
  tagline: Add OpenAI or Anthropic with claudex models open. Switch providers with Claude Code's built-in /model command.
  actions:
    - text: Install Claudex
      link: /en/installation/
      icon: right-arrow
      variant: primary
    - text: View on GitHub
      link: https://github.com/josephismikhail/claudex
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## The default workflow

```powershell
claudex
```

Run `claudex models open` in PowerShell to connect a provider, then launch
`claudex` and use Claude Code's built-in `/model` command. Claudex does not add
a competing `/models` slash command.

<CardGrid stagger>
  <Card title="No profile picker" icon="rocket">
    Bare `claudex` always enters one unified session. Connected accounts persist across exits and restarts.
  </Card>
  <Card title="Local account manager" icon="laptop">
    `claudex models open` opens a bundled loopback page with no remote scripts, fonts, images, or analytics.
  </Card>
  <Card title="Mixed-provider models" icon="random">
    Exact model IDs route GPT requests to OpenAI and Claude requests to Anthropic while the conversation and subagent harness stay intact.
  </Card>
  <Card title="PowerShell native" icon="terminal">
    Windows runs natively with PowerShell. WSL is not required.
  </Card>
  <Card title="Private by default" icon="approve-check">
    Claudex has no collection service, background update check, crash reporter, or provider probe at startup.
  </Card>
  <Card title="Crash-safe state" icon="document">
    Account metadata is written atomically; tokens and API keys remain in the operating system credential store.
  </Card>
</CardGrid>

## Supported account flows

| Provider | Authentication | Catalog |
|---|---|---|
| OpenAI | ChatGPT browser OAuth | GPT-5.6 |
| Anthropic API | Console API key | Models returned for that key by Anthropic's Models API |

Anthropic prohibits third-party gateways from routing Claude Free, Pro, or Max
credentials, so Claudex intentionally uses Console API keys for Anthropic.

Advanced legacy profiles remain available for local models and custom
OpenAI-compatible endpoints, but new users do not need to configure them.
